apiVersion: v1
kind: Service
metadata:
  name: s3
  namespace: {{ rook_namespace }}
spec:
  selector:
    app: rook-ceph-rgw
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  ipFamilyPolicy: PreferDualStack
