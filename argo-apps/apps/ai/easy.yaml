apiVersion: apps/v1
kind: Deployment
metadata:
  name: easy
  namespace: ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: easy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: easy
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu
                operator: In
                values:
                - "true"
      containers:
      - command:
        - /bin/bash
        - /opt/ai-init/easy.sh
        env:
        - name: HOME
          value: /srv/ai
        - name: APP_DIR
          value: /srv/ai/easy
        image: docker.io/ipenguin/ubuntu-24.04-cuda:v1.0.10
        imagePullPolicy: IfNotPresent
        name: primary
        ports:
        - containerPort: 9000
          protocol: TCP
        # first start downloads the installer, python env and models; allow up to an hour
        startupProbe:
          httpGet:
            path: /
            port: 9000
          periodSeconds: 30
          failureThreshold: 120
        readinessProbe:
          httpGet:
            path: /
            port: 9000
          periodSeconds: 30
        resources:
          limits:
            memory: 48Gi
          requests:
            memory: 1Gi
        securityContext:
          privileged: true
          runAsGroup: 1000
          runAsUser: 1000
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /srv
          name: srv
        - mountPath: /usr/lib/x86_64-linux-gnu
          name: usr-lib-x86
          readOnly: true
        - mountPath: /opt/ai-init
          name: ai-init
          readOnly: true
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"
      terminationGracePeriodSeconds: 30
      tolerations:
      - effect: NoSchedule
        key: nvidia.com/gpu
        operator: Equal
      volumes:
      - name: srv
        persistentVolumeClaim:
          claimName: ai-applications
      - hostPath:
          path: /usr/lib/x86_64-linux-gnu
          type: Directory
        name: usr-lib-x86
      - configMap:
          name: ai-init-scripts
          defaultMode: 0755
        name: ai-init
